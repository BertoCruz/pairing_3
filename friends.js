var numbers = [1, 2, 3, 4, 40, 63, 74, 99];

for (var i = 0; i < numbers.length; i++) {
  if  (numbers[i] >= 30){
    console.log(numbers[i]);
  }
}
console.log("-------------------");

for (var i = 0; i < numbers.length; i++) {
  if (numbers[i] > 60) {
    console.log(numbers[i]);
  }
}
